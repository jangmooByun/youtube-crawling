# 0501 사이클 — 0430 중단 이후 신규 사이클 결과

**시작 시각**: 2026-04-30 22:xx (한국 시각, 0430 PAUSED 메모 작성 직후)
**종료 시각**: 2026-05-01 02:xx
**선행**: `PAUSED_2026-04-30.md` 참고. 0430 사이클은 round-2 crawl-links 4/170 처리 후 응답 지연으로 중단.

## 0430 메모와의 차이

PAUSED 메모의 재개 계획("crawl-links 큐 재처리 → deferred 회수 → 신규 분류")은 `influencer.db` 가 살아있다는 전제. 그러나 시스템 어디에도 DB 파일이 남아있지 않아 (gitignore = `*.db`) 미수확 큐(166 priority + 1,760 deferred)는 **영구 소실**. 사용자 합의 후 신규 사이클로 진입.

## 신규 사이클 산출물

| 카테고리 | 0501 신규 | 누적 (data/total/) | 0501 → total +Δ |
|---|---|---|---|
| Fitness | 35 | 175 → 205 | +30 |
| Mindset | 19 | 54 → 73 | +19 |
| Yoga | 18 | 78 → 88 | +10 |
| Nutrition | 9 | 77 → 83 | +6 |
| **합계** | **81** | **384 → 449** | **+65** |

차이(0501 신규 81 vs total 추가 65)는 dedup. 같은 이메일이 기존 0429/0430 풀에도 있는 16 leads.

## 회수율 추이

| 단계 | Profiles | Emails | MX | Personal+MX | Quota |
|---|---|---|---|---|---|
| discover (24 keywords × 2 pages, 4 cat) | 397 | 0 | — | — | 5,348 |
| extract (bio regex) | 397 | 41 | — | — | — |
| crawl-links (162 sources, ~5h) | 397 | 95 | 95 | 64 | — |
| enrich-from-videos (290 ch × 2 calls) | 397 | 127 | 125 | 89 | 580 |
| export (filter has_mx=1 AND is_role_based=0) | — | — | — | **81** | — |

총 quota 사용: ~5,930 / 10,000.

## 0430 → 0501 코드 개선 (커밋됨)

- `src/influencer_finder/extractors/website.py`:`crawl_site_paths` 에 `max_seconds` 파라미터 추가 (per-host wall-clock budget).
- `src/influencer_finder/pipeline.py`:`crawl_external_links` 가 `crawl.per_source_max_seconds` (기본 120s) 를 `crawl_site_paths` 에 전달.
- `config.yaml`: `crawl.per_source_max_seconds: 120` 추가.

→ **검증된 효과**: 첫 4개 source 처리 시간 측정 결과 — skool.com 빠름(~0s), tiffxdan.com 136s 에서 budget 트리거됨, deniseaustin.com 1s (DNS 실패), nourishmovelove 0s (실패). 0430 round-2 처럼 한 host 가 큐를 막는 stall 패턴 재발 안 함.

## 0501 매핑 — 사람 검수 필요

`scripts/mapping_2026_05_01.py` 는 **자동 생성됨**:
- description keyword 패턴 매칭으로 카테고리 1차 분류 (fitness/mindset/yoga/nutrition).
- `niche` / `segment_original` / `industry` / `angle_to_take` 는 **모두 빈 문자열**.
- drop 분류는 0건 (보수적으로 잡음).

**다음 액션**: 81 entries 를 사람이 검토해서 (1) 카테고리 오분류 수정, (2) 각 lead 의 angle_to_take + niche/segment/industry 를 outreach 용으로 채우기. 0430 매핑 파일 (`scripts/mapping_2026_04_30.py`) 의 entry 형식이 참고 모델.

검수 후 `apply_mappings_2026-05-01.py` 재실행 → `data/2026-05-01/{cat}.csv` 갱신 → `merge_2026-05-01_into_total.py` 재실행 → `data/total/{cat}.csv` 의 angle 컬럼이 채워짐.

## 알려진 이슈 / 메모

1. **enrich-from-videos 카운터 underreport**: 스크립트가 `new_emails=4` 로 보고했지만 실제 DB 에는 +32 emails 들어옴. `cur.rowcount` 동작이 SQLite/Python 조합에서 예상과 다른 경우 추정 — 다음 사이클에서 재현 가능하면 카운터 로직 점검.

2. **enrich-from-videos 가 +1,418 sources 추가**: 영상 description 에서 외부 링크 다수 추출됨. 스크립트는 `new_sources=241` 로 보고했지만 DB 는 1,597 - 162 = 1,435 추가. 같은 카운터 문제. 이 1.4K sources 는 모두 `fetched_at IS NULL` 상태 — 다음 사이클에서 추가 crawl-links 로 더 많은 emails 회수 가능.

3. **export by_email dedup 으로 89 → 81 손실 (8건)**: 같은 이메일을 쓰는 다중 채널 — 가장 follower 큰 채널만 살아남음. PAUSED 메모의 "159 vs 136" (23 차이) 와 같은 메커니즘. 의도된 동작 (outreach 용 unique email per row).

4. **0430 미수확분 (1,760 deferred + 162 priority) 영구 포기됨**: 신규 사이클의 빠른 yield(81 leads, 5,930 quota) 가 deferred 큐 회수보다 효율적이라 판단. 사용자 합의됨.

## 다음 사이클 진입 시 추천

1. **enrich-from-videos 가 만든 1,435 신규 sources 를 한 번 crawl-links** — 추가 emails 기대치 낮지 않음. 120s budget 로 ~5h.
2. **신규 키워드 풀 추가 discover** — 0429 + 0430 + 0501 에 사용된 키워드 셋 합쳐서 `searched-list.md` 갱신, 새 카테고리 (예: 명상, 웰니스 코치) 키워드 추가.
3. **mapping_2026_05_01.py 의 81 entries 를 사람 검수** + angle 채우기 → apply + merge 재실행.

## 참고 파일

- 신규: `scripts/discover_2026-05-01.py`, `scripts/enrich_from_videos_2026-05-01.py`, `scripts/mapping_2026_05_01.py`, `scripts/apply_mappings_2026-05-01.py`, `scripts/merge_2026-05-01_into_total.py`, `data/2026-05-01/{all,fitness,mindset,yoga,nutrition}.csv`.
- 수정: `src/influencer_finder/extractors/website.py`, `src/influencer_finder/pipeline.py`, `config.yaml`.
- 보존: `PAUSED_2026-04-30.md` (히스토리 레퍼런스).
